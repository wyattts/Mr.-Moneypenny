### Changes to implement:
1. On the insights dashboard, the text in the daily allowance box goes outside of the box. Implement text wrapping to keep text inside it's box for all of the KPIs.
2. When there are only a couple of expenses in the spending by category bar chart, the bars are huge and chunky, and only scale down when more expenses are added. Make the bars a uniform, smallish size so that the bars themselves don't change with additions to the budget ledger, only the size of the chart itself. 
3. Remove the text explaining the color coding from the bar chart. It's intuitive enough.
4. The app only has support for monthly budgeting. Therefore, the dropdown on the insights page should allow users to select a calendar month, not year/quarter/ytd/month.

### New features
1. A second line chart with a line of best fit for variable expenses, essentially showing the user whether they're on track for the month with variable spending. 
2. In the "Categories" tab, a "Sum Total" box showing the sum of the individual budget parts, as well as Sum Total boxes for each category (fixed, variable, saving/investing)
